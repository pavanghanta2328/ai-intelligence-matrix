services:
  - type: web
    name: ai-intelligence-matrix
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run Gather_updates.py --server.port $PORT
    envVars:
      - key: mongo_uri
        sync: false
